# 人物优先标签部署验收

- 发布标识：`media-character-tags-20260912-v1`。
- 实际线上执行链路：独立 task-executor-service；旧 mytools-backend 处于停用状态，本次未启动。
- 标签脚本：1.3.0；默认模型：huihui_ai/qwen3-vl-abliterated:8b；默认策略：media-tags-v2。
- 7 个标签任务步骤已更新，执行器已加载独立包目录；旧发布目录和历史包保留。
- 391 个包文件的大小与 SHA-256 均通过校验。
- Executor、Scheduler、Ollama 均为 active；未完成任务中显式指定旧 4b 的数量为 0。

## 真实推理

使用服务器上已发布脚本和执行器生效配置调用 Ollama；不使用业务私人素材。

| 人工样本 | 耗时 | 输出 |
| --- | --- | --- |
| 人物描述文本 | 9.60 秒 | 黑发、白围巾、圆眼镜、微笑挥手、开朗勇敢、公园场景 |
| 合成人物图片 | 11.93 秒 | 黑发、红上衣、圆眼镜、平脸 |

两次均成功解析 JSON，并生成符合现有标签结构的结果，人物特征位于前列。样本数量有限，不能据此推断全量准确率或批处理吞吐量。

## 验证边界与回滚

通过现有下载入口服务身份创建独立 media_generate_tags 旁路任务时，Scheduler 返回 403。未改变权限配置；本次真实推理验证直接执行发布脚本，未完成调度创建到结果落库的端到端验收。

服务器审计目录：`/opt/yuyutian/mytools/runtime/media-character-tags-20260912`，含变更前快照、部署记录、真实推理结果及 `rollback.py`。回滚需由管理员在执行器空闲时显式执行；不会自动重打旧标签。

发布发现同名历史任务的版本唯一约束冲突，首次事务已回滚。后续按版本降序递增后成功；本地 V148、V149 迁移已同步修正顺序。线上本次采用有审计的参数化定向更新，未新增 Flyway 历史记录，未替换 Scheduler JAR。
