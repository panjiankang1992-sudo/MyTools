# 全量电子书分析：来源可用性检查

> 2026-09-13 最新决定：用户要求停止剩余分析并使用已有结果。任务已停止，保留 13,173 块；已依据 74 本完整书生成新模板候选，1 本部分书另计。见 `2026-09-13-enriched-novel-existing-results.md`。此前“等待全量或异常书修复”的生成前提已被用户本次明确缩减范围的要求替代。

> 后续质量整改：旧任务已暂停，证据版两书验证通过后恢复全量采集。当前进度应读取 `ebook-style-evidence-v2.1-20260912/status.json`；本文下述旧任务进度是历史快照。详见 `2026-09-12-enriched-novel-quality-rework.md`。

## 状态

用户要求全部读取、分析后再生成。已完整读取登记文件并完成解析检查，启动可解析部分的全文分块任务；这不等于模型已经分析完成。两本源文件异常未解决前，禁止生成全量版模板。先前抽样报告不能作为全量分析结果。

## 2026-09-12 实测

- 通过当前本地 SSH 配置连接远程主机，仅执行读取和临时检查脚本，不修改生产数据库或原书。
- 当前用户电子书登记共 184 本：156 本 TXT（357,367,183 字节）、28 本 EPUB（217,155,642 字节）。登记大小合计约 548 MiB，不代表文件实际可读。
- 逐条查询迁移资产对应的位置：183 本的旧登记路径不存在；继续核查发现其实际搬迁至 `/opt/extend/resource/yuyutian/ebook`。本轮完整读取搬迁文件，183 本大小和 SHA-256 均与登记一致，无需改生产数据即可分析。
- 另 1 本为托管导入来源。全量准备脚本再次读取并核验其大小、SHA-256。184 本文件均已读取，不能把文件读取等同于模型分析完成。
- 两个启用的远程存储连接中，一个根目录列举返回 HTTP 502；另一个根目录可列举，检查“在阅”“已阅”未定位到电子书文件。这不是对整个网盘递归扫描完成的声明。
- 182 本可完整解析为正文，分为 37,314 个连续块；TXT 严格解码，EPUB 按 spine 阅读顺序提取全部正文资源，跳过脚本和样式。
- 两本未通过：`0c04330d-65c0-5ee6-98c1-e109c5c10b03`（TXT，458,738 字节）无法按 UTF-8、GB18030、Big5、UTF-16 严格解码，自动编码识别无结果；`c67c3056-067f-57a4-a5b8-24b1526c17b0`（EPUB，18,134,294 字节）存在 54 个 ZIP 本地头，但没有中央目录或结束记录，无法证明正文完整。二者哈希与登记一致，说明不是本轮读取造成变化。

## 批处理与验证

- 实现：`scripts/analyze_ebook_corpus.py`；本地四项测试通过，覆盖连续全文分块、严格编码与 BOM、EPUB 阅读顺序、未知格式拒绝。
- 私有远程状态目录：`/opt/yuyutian/mytools/runtime/ebook-style-full-20260912`。`manifest.json` 保留来源映射；`coverage.json` 记录完整正文摘要、码点数、分块数；`progress.sqlite3` 逐块保存结果；`receipts.jsonl` 记录实际输入输出 token 和调用耗时；`status.json` 显示阶段与进度。
- 模型沿用当前 8B，通过 `/api/generate` 的原始消息格式调用。每次只处理一个块，不截断超长书、不选择几个片段替代全文；分层归纳纳入每个分块的观察。仅提取非露骨文学技巧，不生成正文或复制原句。
- `--analyze-readable` 允许先处理 182 本，错误书仍留在总清单。若错误未修复，任务最终进入 `WAITING_SOURCE_REPAIR_NOT_GENERATED`，不会以部分结果生成或发布模板。成功块落盘后重跑复用，模型异常则停止，不循环重试。
- 后台任务不占用当前对话连接，但不代表已分析完成；后续应读取实际状态和收据再报告进度。未设置定时通知。
- 启动后首轮实测：进程在运行，状态 `ANALYZING`，20/37,314 块完成，0/182 本完成。最近两次实际调用分别输入 3,547 / 3,510 tokens，输出 179 / 283 tokens，耗时约 3.96 / 5.44 秒，均以 `stop` 正常结束。这只是当时快照，不是实时进度；按首批速度粗估为天级任务，仍有逐书和全库汇总开销。

## 继续条件与验收口径

需要两本异常书的可解码/完整版本，或明确同意将其排除出最终资料范围。不能静默排除，也不能以在线书架的部分章节代替电子书全文。当前实际分析范围是这 184 本登记电子书，并非整个网盘内所有可能未登记的电子书。

可读书籍先用当前 8B 模型逐书、逐块覆盖全文，记录来源摘要、文本范围、模型调用结果和失败项，保存断点；最后核对每本正文覆盖后才汇总风格模板。参考 GitHub 的表达增强与剧情一致性检查方法，不复制原书段落。新来源必须重新核验并更新本次任务清单，不能用不同内容覆盖旧哈希后假称原文件分析成功。

遵循 `add-adaptation-template` 技能：全量证据不足时不发布新模板；后续发布只能走独立管理令牌保护的管理 API，不能绕过接口写数据库。
