# 参数选中态修复

- 原因：Choice 构建器接收按值传入的 selected 布尔值。参数已经改变，但复用的按钮没有重新计算样式。
- 修复：构建器直接读取 imageSize、count、mode 状态，保持点击逻辑与样式依赖一致。
- 验证：assembleHap 成功；模拟器和真机逐项点击横版、竖版、方形、4 张、2 张、1 张，断言新选项背景为选中绿色，旧选项恢复默认背景。
- 已覆盖安装真机；验证未提交新的生成请求。
