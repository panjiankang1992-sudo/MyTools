# MyTools 服务化迁移差距分析报告（v4 · 最新代码）

> 分析范围：`service/design/` 设计文档、当前目录旧版单体（`src/`）、`~/mycode/DownloadBot`、`service/*-service/` 新版微服务实现。
> 方法：代码级交叉比对（读 connector / 下载脚本包 / automation / gateway / MCP 源码 + 全量 grep）。
> **快照时间**：本版在 v3 之后再次读取最新代码，本轮新增提交主要补齐 MCP 与 LAN 连通性。

---

## 0. 相对 v3 的变化（本轮代码更新）

v3 报告剩余的两个明确缺口（MCP、connectivity）已被最新提交关闭：

| v3 缺口 | 最新状态 | 证据 |
| --- | --- | --- |
| stdio MCP `analyze_download` | ✅ 已迁移 | 新增 `download-ingestion-service/src/mytools_download_ingestion/mcp_server.py`（210 行）+ `tests/test_mcp_server.py`；commit `c4b1cd4 feat(platform): restore MCP and LAN connectivity` |
| connectivity（局域网发现/mDNS） | ✅ 已迁移到 Gateway | 新增 `mytools-gateway` 的 `ConnectivityController` / `ConnectivityService` / `ConnectivityModels` + `ConnectivityServiceTest` |

---

## 1. 当前剩余差距（几乎收敛为零）

### 1.1 DownloadBot → 新服务：24/24 能力全部迁移 ✅

v3 的"MCP 唯一未迁移"已关闭。DownloadBot 的 `mcp_server.py`（`analyze_download` 工具）现等价迁移到 Download Ingestion 的 `mcp_server.py`：实现有界 JSON-RPC（`tools/list`/`tools/call`）、`analyze_download` 工具、链接分类（magnet/HTTP(S)，经既有编排任务类型覆盖网页归档与 X 帖子）、`mode=auto/pikpak/local` 三种策略。至此 DownloadBot 24 项能力全部在新服务落地。

### 1.2 旧单体 → 新服务：仅剩 2 个模块未迁移

| 旧模块 | 状态 | 说明 |
| --- | --- | --- |
| **copilot**（Copilot Gateway 流式代理） | ❌ 未迁移 | 仅存在于旧 `src/main/java/.../copilot/`，27 篇设计文档零提及 |
| **openapi**（Open API，`OpenApiController`/`OpenProfileResponse`） | ❌ 未迁移 | 仅存在于旧 `src/main/java/.../openapi/`，设计文档零提及 |

> 注：`config/OpenApiConfig.java` 是 Springdoc/Swagger 配置，与业务 `openapi` 模块无关；两者都未迁移到新服务。

### 1.3 需核验的细微对等点（非明确缺失）

| 项 | 说明 |
| --- | --- |
| 专辑标题是否含"模型生成标题" | 旧 DownloadBot 用 Ollama 视觉模型生成短标题 + 规则回退；新实现用 `albumTitleText`（消息/网页标题/正文线索）+ sha256 摘要，未发现模型调用 |
| token 强制下线能力 | Identity 会话已有注销；"强制下线其他会话"是否完整需核验 |
| cloudfile Alist/WebDAV 流式播放/缩略图 | 账户迁移为禁用 native provider；流式播放兼容需核验 |

### 1.4 设计文档明确"有意延后"（不变）

DSH 全协议 RPC/WS/SSE、Storage/Drive 协议原生树级写操作、K8s/服务网格/消息中间件、灰度/双写（无活跃用户）、每脚本拆微服务。

---

## 2. 迁移完成度总表（最新代码）

### DownloadBot（Python 旧服务）→ 新服务

| 能力 | 状态 |
| --- | --- |
| Telegram 长轮询 + 7 类媒体 + text_link + Local Bot API + 相册聚合 + 回复拆解 | ✅ |
| QQ Gateway + C2C/群/频道 + 心跳/resume + 多层/引用/复合消息(102)递归拆解 | ✅ |
| OneBot WS + `get_forward_msg` 展开 + `get_file` STREAM | ✅ |
| PikPak 离线 + 周期 watcher + 稳定观察 + 受控移动 | ✅ |
| magnet → PikPak / aria2 本地断点 | ✅ |
| HTTP / X / 网页归档 / 消息附件 / 本地导入 下载 | ✅ |
| 专辑目录 + 标题（`{title}--{sha256}`） | ✅ |
| 下载进度通知（>10MiB/每5%/完成，里程碑去重） | ✅ |
| 标签独立重试 + 打标签后回发 | ✅ |
| NapCat 二维码登录恢复 | ✅ |
| MySQL durable queue（幂等/SHA-256 去重） | ✅（概念迁移） |
| **stdio MCP `analyze_download`** | ✅（本轮补齐） |

### 旧 MyTools 单体 → 新服务

| 模块 | 状态 |
| --- | --- |
| appmarket / auth / drive / feedback / localfile / media / reader / role / user / webdav / scheduler / connectivity | ✅ 已迁移 |
| dsh / cloudfile / token | ⚠️ 部分（详见 1.3 与 1.4） |
| **copilot / openapi** | ❌ 唯一未迁移 |

---

## 3. 结论

1. **DownloadBot 已 100% 迁移**（24/24 能力，含 MCP）。下载、渠道连接、媒体分析、通知等全部功能在新微服务有对应实现。
2. **旧单体仅剩 copilot、openapi 两个模块未迁移**，且二者在 27 篇设计文档中零提及——这是最后的设计盲区，需业务方明确处置（保留只读 / 补建服务 / 弃用）。
3. **"已迁移 ≠ 已启用"**：所有新服务/路由/任务默认 `ENABLED=false`，旧 DownloadBot worker 与旧单体仍是权威执行路径。数据迁移已通过远程验收（`design/26`），**功能切流尚未执行**——这是当前最大的"差距"已从"代码缺失"转为"尚未切换"。
4. **代码仍在演进**：一天内新增约 20 个提交，缺口从 v2 的十多项 → v3 的 1+3 项 → v4 的 2 项。结论时效性强，正式验收前建议再复跑代码级比对。
