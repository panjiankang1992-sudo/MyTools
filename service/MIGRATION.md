# 服务化迁移执行清单

## 阶段 1：基础工作区

- [x] 建立各服务独立目录。
- [x] 建立总体与服务设计。
- [ ] 固化任务创建、状态、脚本上下文和结果 Schema。
- [ ] 实现 Scheduler 与 Executor MVP。
- [ ] 创建各服务独立 schema 和最小权限账号。

## 数据迁移原则

- 必须迁移：用户、权限、会话必要状态、业务资产关系、书架、阅读进度和用户配置。
- 优先迁移：下载记录、媒体目录、消息记录和可稳定映射的任务历史。
- 允许重建：缩略图、标签、视频截图、简介、搜索缓存、章节缓存、远端文件索引和临时任务日志。
- 无法验证的数据不强行迁移；旧库进入只读保留期，并通过新任务重新生成可再生数据。
- 每个迁移脚本必须提供 dry-run、幂等键、批次游标、迁移报告和对账 SQL。

## 阶段 2：首个旁路任务

- [ ] 封装 `media_generate_tags` 脚本包。
- [ ] MyTools 以关闭为默认值的开关创建新任务。
- [ ] 新旧结果双跑对账，不改变线上读取结果。
- [ ] 验证超时、取消、重试和幂等。

## 阶段 3：主要耗时任务

- [ ] 下载任务。
- [ ] 媒体扫描、缩略图、截图和简介。
- [ ] 书源搜索、发现、电子书导入和索引。

## 阶段 4：共享能力

- [ ] Messaging 和 Message Automation。
- [ ] Asset Registry 和 Storage Gateway。
- [ ] Media、Drive、Reader、Identity 独立服务。

## 阶段 5：切流与收尾

- [ ] 分租户或按任务类型灰度。
- [ ] 校验结果、性能、任务积压和错误率。
- [ ] 停止旧 Job，但保留快速回退。
- [ ] 观察期结束后删除旧执行路径。
