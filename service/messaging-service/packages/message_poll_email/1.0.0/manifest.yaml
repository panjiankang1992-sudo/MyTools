name: message_poll_email
version: 1.0.0
runtime: python3.12
entrypoint: scripts/main.py
capabilities:
  - network_http
parameters:
  required:
    - accountKey
result:
  schema: schemas/result.schema.json
limits:
  timeoutSeconds: 300
  maxOutputBytes: 65536
