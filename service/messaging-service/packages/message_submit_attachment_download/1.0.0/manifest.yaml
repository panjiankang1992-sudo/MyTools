name: message_submit_attachment_download
version: 1.0.0
runtime: python3.12
entrypoint: scripts/main.py
capabilities:
  - messaging_internal_execute
parameters:
  required:
    - attachmentJobId
result:
  schema: schemas/result.schema.json
limits:
  timeoutSeconds: 60
