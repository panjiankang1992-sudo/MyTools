spring:
  datasource:
    url: jdbc:h2:mem:dsh_connector;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway.locations: classpath:db/migration
dsh-connector.internal-token: test-token
