spring:
  datasource:
    url: jdbc:h2:mem:media_library;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway.locations: classpath:db/migration
media-library.internal-token: test-token
