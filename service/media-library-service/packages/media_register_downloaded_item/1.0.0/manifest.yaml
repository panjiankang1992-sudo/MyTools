name: media_register_downloaded_item
version: 1.0.0
runtime: python3.12
entrypoint: scripts/main.py
capabilities: [media_library_write]
parameters:
  required: [ownerId]
result:
  schema: schemas/result.schema.json
limits:
  timeoutSeconds: 60
  maxOutputBytes: 65536
