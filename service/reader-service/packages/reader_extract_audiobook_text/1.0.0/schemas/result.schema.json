{
  "type": "object",
  "required": ["generationId", "projectedChapterCount", "projectedCharacterCount"],
  "properties": {
    "generationId": {"type": "string"},
    "projectedChapterCount": {"type": "integer", "minimum": 1},
    "projectedCharacterCount": {"type": "integer", "minimum": 1}
  },
  "additionalProperties": false
}
