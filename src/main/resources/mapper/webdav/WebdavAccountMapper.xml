<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuyutian.mytools.webdav.mapper.WebdavAccountMapper">

  <resultMap id="WebdavAccountResultMap" type="com.yuyutian.mytools.webdav.model.WebdavAccount">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="type" property="type"/>
    <result column="name" property="name"/>
    <result column="url" property="url"/>
    <result column="username" property="username"/>
    <result column="password" property="password"/>
    <result column="is_active" property="isActive"/>
    <result column="is_default" property="isDefault"/>
    <result column="create_time" property="createTime"/>
    <result column="update_time" property="updateTime"/>
  </resultMap>

  <select id="selectById" resultMap="WebdavAccountResultMap">
    SELECT * FROM webdav_account WHERE id = #{id}
  </select>

  <select id="selectByUserId" resultMap="WebdavAccountResultMap">
    SELECT * FROM webdav_account WHERE user_id = #{userId} ORDER BY is_default DESC, id ASC LIMIT 1
  </select>

  <select id="selectAllByUserId" resultMap="WebdavAccountResultMap">
    SELECT * FROM webdav_account WHERE user_id = #{userId} ORDER BY is_default DESC, id ASC
  </select>

  <select id="selectDefaultByUserId" resultMap="WebdavAccountResultMap">
    SELECT * FROM webdav_account WHERE user_id = #{userId} AND is_default = 1 LIMIT 1
  </select>

  <select id="selectActiveAlistByUserId" resultMap="WebdavAccountResultMap">
    SELECT * FROM webdav_account
    WHERE user_id = #{userId} AND type = 'alist' AND is_active = 1
    ORDER BY is_default DESC, id ASC
    LIMIT 1
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO webdav_account (user_id, type, name, url, username, password, is_active, is_default)
    VALUES (#{userId}, #{type}, #{name}, #{url}, #{username}, #{password}, 1, #{isDefault})
  </insert>

  <update id="updateByUserId">
    UPDATE webdav_account
    SET type = #{type}, name = #{name}, url = #{url}, username = #{username}, password = #{password}
    WHERE user_id = #{userId}
  </update>

  <update id="updateById">
    UPDATE webdav_account
    SET type = #{type}, name = #{name}, url = #{url}, username = #{username},
        password = #{password}, is_active = #{isActive}, is_default = #{isDefault}
    WHERE id = #{id}
  </update>

  <update id="clearDefaultByUserId">
    UPDATE webdav_account SET is_default = 0 WHERE user_id = #{userId}
  </update>

  <delete id="deleteById">
    DELETE FROM webdav_account WHERE id = #{id}
  </delete>

  <update id="updatePasswordById">
    UPDATE webdav_account SET password = #{password} WHERE id = #{id}
  </update>

  <select id="selectMigrationBatch" resultMap="WebdavAccountResultMap">
    SELECT * FROM webdav_account WHERE id &gt; #{afterId} ORDER BY id ASC LIMIT #{limit}
  </select>

</mapper>
